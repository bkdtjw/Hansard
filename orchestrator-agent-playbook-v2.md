# 编排系统 · 编码 agent 驱动手册 v2（多agent施工版）

本版**全文替换 v1**。配套 `orchestrator-spec.md`（下称 spec）使用。分工不变：spec 定"做什么"（agent 只读），CLAUDE.md 定"怎么干活"（入库、每会话在场），开工提示词定"这一段干哪些"。v2 的变化：施工组织从单 agent 串行改为 **Lead + 任务卡派发**的多 agent 模式，施工协议写进 CLAUDE.md 以便跨会话生效。

多 agent 在这里买到的东西，按真实价值排序：一、每张任务卡一个全新的小上下文（对抗长会话漂移——与 spec §6 对抗的是同一个敌人）；二、独立评审（写码者不自评）；三、有条件的吞吐并行（仅写域不相交时）。成本：Lead 的整合与审计开销。人工验收四步（§4）不因多 agent 而减少一步。

## 0. 一次性准备

1. 建仓库，spec 放根目录，只读；改 spec 走人工修订并升版本号。
2. §1 的 CLAUDE.md 放仓库根（Codex 则同内容存为 AGENTS.md）。
3. `echo M0 > MILESTONE`；建空的 `IMPLEMENTATION_NOTES.md`、`QUESTIONS.md`。
4. 分支纪律：每里程碑一个分支 `m0`、`m1`……验收通过合入 main 并打 tag。
5. 施工用 worktree（模式二并行时）建在仓库外侧邻目录 `../wt-T{k}`，用完 `git worktree remove`——这是施工脚手架，与产品自身的 worktree 概念（spec §4.5）无关，勿混放。

## 1. CLAUDE.md 全文（复制即用，一字不改写入仓库根）

```markdown
# 项目铁律（每次会话都生效）

## 唯一事实来源
- 本项目实现 `orchestrator-spec.md`（下称 spec）。spec 是宪法，只读。
  发现 spec 内部矛盾或无法实现之处：停下报告，禁止擅自修改或绕过。
- 本文件只规定工作方式，不复述 spec 内容；凡与 spec 冲突，以 spec 为准。

## 工作方式
- 严格按 spec §15 里程碑推进；当前里程碑写在 `MILESTONE` 文件里。
  禁止实现后续里程碑的内容，即使"顺手"。
- 测试先行：先把本里程碑验收测试写出来并展示"红"，再实现到"绿"。
- commit 粒度与格式：每张任务卡收尾一个 commit，`m{n}/T{k}: <做了什么>`；
  无卡号的 Lead 胶水提交用 `m{n}: <做了什么>`。
- spec §16 反模式清单 13 条是硬约束，触及相关代码时逐条自查。
- 依赖只允许 spec §14 白名单；新增任何依赖前先提问。

## 决策与提问协议
- 属于 spec §17 开放决策点的问题：自行决定，
  记入 IMPLEMENTATION_NOTES.md（决定 + 一行理由）。
- spec 未定义且不在 §17 的问题：停止该子任务，把问题与你建议的
  2–3 个选项按 QUESTIONS.md 既有格式写入，转做不受影响的子任务；
  本轮回复末尾集中列出所有待决问题。
- 协议字段、DDL、落盘顺序上禁止做任何 spec 之外的"改进"。

## 会话冷启动（每个新会话的第一步，不可跳过）
1. 读 spec 的"如何使用本文档" + §15 当前里程碑行 + 该里程碑涉及章节
2. 读 IMPLEMENTATION_NOTES.md 与 QUESTIONS.md
3. 跑 `pytest -q` 确认接手时的绿基线（首个会话尚无测试，视为绿）；
   不绿先报告，不动代码
4. 输出本会话计划（含任务分解表），等确认后动手

## 多agent施工协议（Lead 会话适用）
角色：
- Lead（主会话）：拆解里程碑为任务卡、派发子agent、整合产物、写域审计、
  运行完成标准、升级问题。禁止亲写实现代码；集成胶水（包骨架、__init__、
  装配代码）除外，且每一处记入 IMPLEMENTATION_NOTES.md。
- worker：一次性子agent，只做一张任务卡，做完即弃。
- 评审agent：只读，输入仅为 spec + git diff + spec §16 清单，产出
  文件:行号 级报告；只报告不修改。

派发规则：
1. 任务卡是子agent的唯一输入契约。不得假设子agent能看到本文件或对话
   历史——一切约束写进卡内。格式：
     任务卡 T{k}
       目标: <一句话>
       只读输入: spec §… ；已冻结接口: <文件/签名>
       可写路径: [<白名单>]（越权 = 整卡拒收）
       依赖: T{m} 或 无
       完成标准: <命令 + 期望，如 pytest tests/test_store.py -q 全绿>
       禁止: 修改 spec / CLAUDE.md / MILESTONE；触碰白名单外路径；
             安装 spec §14 白名单外依赖；再派发子agent
2. 并行仅当同批任务的可写路径两两不相交（spec §5.1 的同一判据）：
   - 模式一（默认）：串行派发，同一检出目录，每卡收尾单独 commit。
   - 模式二（须在分解表中显式标注并经人类批准）：每卡一个 git worktree
     （../wt-T{k}，分支 m{n}-T{k}），Lead 按依赖序合并。
3. 每卡收尾三步，缺一不可：
   a) `git diff --stat` 对照可写路径审计——越权则整卡拒收、回滚、重派；
   b) 运行该卡完成标准命令，以退出码为准；
   c) commit。子agent 的自述只是路由信号，不作数。
4. 同一张卡失败重派 ≤ 2 次；仍失败 → 停止该卡，写入 QUESTIONS.md
   升级人类，禁止继续换姿势重试。
5. 全量集成通过后必须派独立评审agent；评审发现的问题回环给原卡 owner
   重派修复，Lead 与评审都不得代改。
6. 子agent 之间禁止任何直接协作或通信；协作只通过落盘产物与测试。

## 完成定义（每里程碑，五项俱备才允许宣布完成）
1. 全量 pytest 通过，贴完整输出，不许只贴摘要
2. 逐条对照 spec §15 该里程碑验收标准给出证据（命令 + 输出）
3. 逐条对照 spec §16 自审：给出 文件:行号 级证据，或写明"不涉及"
4. 独立评审报告存在，且遗留问题为零或已逐条裁决记录
5. IMPLEMENTATION_NOTES.md 已更新（含 Lead 胶水清单与开放决策）
```

## 2. 施工组织说明（给你，不入库）

- 协议是 spec 三条原则的移植：写域不相交才并行 → 任务卡白名单 + 两模式；不信汇报 → 每卡收尾三步 + 人工验收不变；聊天是协调层、文件是真相层 → 子agent 互不通信、禁止嵌套派发，协作全走产物。
- 强制手段的诚实边界：评审agent 的"只读"可以在工具层机械强制——用 Claude Code 的 subagent 定义文件限制其工具集为只读（定义文件的具体语法以本机 `/agents` 命令或官方文档实测为准，实测结果记 NOTES）；worker 的写域白名单做不到沙箱，走收尾三步的 git diff 事后审计、越权整卡拒收——对应 spec §8.2 的同一简化决策。
- 模型分配：Lead 与评审用强模型（如 Opus 4.8），机械型任务卡（照 DDL 建库、包骨架）可降档，派发时按卡指定。
- 模式选择：默认模式一。模式二只在写域确实不相交、且单卡耗时长到值得并行时用；它把合并成本转给 Lead，卡少时不划算。

## 3. 开工提示词

### 3.1 M0 · Lead 版全文（后续里程碑套同一六步骨架）

```
读 CLAUDE.md（含多agent施工协议）并执行冷启动流程。本会话你是 Lead。

第0步（幂等自检）：确认仓库根存在 orchestrator-spec.md（只读）、
CLAUDE.md、MILESTONE（内容 M0）、IMPLEMENTATION_NOTES.md、QUESTIONS.md，
当前分支 m0。缺任何一项：报告并停止，禁止自行补造 spec。

本会话目标：完成 spec §15 的 M0（协议 + 存储 + 核心环），仅此而已。
涉及章节：§3 协议、§4 状态层（DDL 照抄，禁止"优化"字段）、
§5.1 核心循环（单线程串行 + mock）、§7.1 适配器接口、
§7.4 mock 型（ledger 落盘现在就做，M4 依赖它）、§9.1 恢复算法、
§14 技术选型、附录 A、附录 B。

第一步：产出 M0 任务分解表（任务卡格式，标注依赖与串/并行模式）。
参考分解如下，可调整但须逐处说明理由；未经我批准不得派发。
  T0 包骨架（Lead 胶水，仅此一次）：src/{protocol,store,scheduler,
     render,adapters,verify,cli}/ 空模块 + pyproject + pytest 配置
  T1 验收测试先行（见红）：附录B事件序列断言、§9.1 恢复各情形
     （挂起保持 + a–c）、附录A schema 校验 —— 可写: tests/
  T2 协议层：信封结构、schema 校验、type/保留策略/发送者约束
     —— 可写: src/protocol/
  T3 存储层：DDL 建库、事件追加、派发表、§4.4 事务边界
     —— 可写: src/store/
  T4 mock 适配器 + ledger 落盘 —— 可写: src/adapters/
  T5 调度核心环（串行版；依赖 T2–T4）—— 可写: src/scheduler/
  T6 E2E 装配：附录B fixture 跑绿（依赖全部）—— 可写: tests/fixtures/
     + 记录在案的胶水
  说明：T2/T3/T4 写域不相交，可标模式二并行；T1 先行；T5、T6 串行。

第二步：派 T1，把"红"的完整输出贴给我。
第三步：按批准的表派发实现卡；每卡收尾执行协议第 3 条三步。
第四步：全量 pytest；失败先定位归属卡，按协议第 4 条重派或升级。
第五步：派独立评审agent（只读，输入仅 spec + diff + §16 清单），
收 文件:行号 报告；问题回环原卡 owner 修复。
第六步：按 CLAUDE.md 完成定义出验收报告，停下，等我人工验收。

明确不做：视图组装（M1）、真实 CLI/API 适配器（M2）、
resume/聚合/并行/多线程（M3）、混沌 harness 完整化（M4）、
任何 spec §12 CLI 命令（E2E 用 pytest fixture 驱动）。
```

### 3.2 M1–M4 参数表

六步骨架不变，替换目标、涉及章节、参考分解、明确不做。每个里程碑第一步都是任务分解表 + 人工批准，不得跳过。

| 里程碑 | 涉及章节 | 补充边界 | 明确不做 |
|---|---|---|---|
| M1 | §6 全部；§5.2 触发源、§5.3 看门狗、§5.4 终止；§11.2–11.3 | moderator 仍绑 mock；视图渲染须有快照测试；看门狗测试用注入假时钟 | 真实后端；resume；聚合与并行 |
| M2 | §7.2–7.3 冷启动、§7.6；§8；§10；§5.5；§11.1；§12 的 run/new/send/chat/status/approve/reject/stop | 真实后端 ≥2 家（以本机安装为准）；CLI flag 以 --help 实测，差异记 NOTES；"挂起停机重启续跑"做成可复现脚本 | resume 热续；聚合；并行；多线程 |
| M3 | §6.5；§7.5；§5.1 聚合与写域并行；§5.2 作业回调；§9.3；§12 的 attach/bench | bench 报告落文件；并行用写域相交/不相交两组用例 | 混沌完整化 |
| M4 | §9.4 两层混沌；§13 埋点与 orch metrics；§12 的 replay/reopen | 注入钩子覆盖 §4.4 每个事务间隙；mock 层 ≥50 轮 100% 是硬门槛 | — |

注意：spec §15 未把 §12 CLI 分配进里程碑，上表是补充分配，让 Lead 记入 NOTES。

## 4. 里程碑验收规程（人工执行；一切 agent 报告只是路由信号）

0. 先核对两份材料**存在且具体**：每张卡的写域审计记录、独立评审报告（须引用到文件:行号）。缺任一，直接退回，不进入下面四步。
1. 你自己跑 `pytest -q`，以退出码为准。
2. 反模式抽查（线索级 grep，命中后人工判读）：
   - `grep -rn "findall\|re\.search" src/ | grep "@"` —— §16.1
   - `grep -rn "time.sleep\|asyncio.sleep\|threading.Timer" src/` —— §16.2，逐处判断轮询等待（可）还是拿 sleep 当看门狗（禁）
   - `grep -rn "langchain\|langgraph\|autogen\|crewai" pyproject.toml src/` —— §16.13
   - 其余 10 条对照评审报告与 Lead 自审的 文件:行号 证据核对。
3. `git diff main...m{n}` 通读，重点 store/ 与 scheduler/。
4. 通过：合入 main，打 tag，`echo M{n+1} > MILESTONE`。不通过：把失败输出**原样**贴回 Lead 会话，不附加猜测。
5. 清空 QUESTIONS.md：能裁决的写"裁决 + 理由"归档进 NOTES；裁决不了的说明 spec 有洞，走人工修订。

## 5. 会话管理

- Lead 上下文告急（复述早先内容、忘记已定决策、测试改来改去）：令其把进度与未竟任务卡状态追加进 NOTES，然后换届开新 Lead 会话——冷启动流程 + 盘上任务卡状态保证无损接手。worker 天然短命，无此问题。
- M2 仍是最费人力段：真实 CLI 的 flag、session_id 提取要现场实测，安排整块时间陪跑。
- 中途加需求：人工改 spec、升版本号，再对 Lead 说"spec 已更新至 vX.Y，diff 如下，评估影响后再动"。

## 6. QUESTIONS.md 单条格式（写进任务卡与开工词，要求一律遵守）

```
Q{n} [{里程碑}/T{k}] {一句话问题}
  背景: spec 哪一节缺失或矛盾
  选项: A … / B …（必须附建议与理由）
  裁决: {人填} → 归档进 NOTES 或触发 spec 修订
```
